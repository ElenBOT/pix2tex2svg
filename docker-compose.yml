
services:
  pix2tex2svg:
    build: .
    container_name: pix2tex2svg
    ports:
      - "${PORT:-7071}:${PORT:-7071}"
    environment:
      - ENABLE_HTTPS=1
      - PORT=${PORT:-7071}
    volumes:
      - .:/app
    restart: unless-stopped
